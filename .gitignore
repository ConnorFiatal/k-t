# Environment / secrets
.env
.env.*
!.env.example

# Private keys & certificates (SAML/TLS material belongs in env vars or a
# secrets manager, never in the repo)
*.pem
*.key
*.crt
*.cer
*.der
*.p12
*.pfx
*.keystore
*.jks
id_rsa
id_dsa
id_ecdsa
id_ed25519

# Credential / secret files
secrets.json
credentials.json
service-account*.json
*.secret
.npmrc

# Cloud provider credentials
.aws/
.gcloud/
gcloud-*.json
*.tfvars

# OS / editor cruft
.DS_Store
Thumbs.db

# SQLite runtime files
data/*.db-wal
data/*.db-shm

# Session store (connect-sqlite3)
db/sessions.db
db/sessions.db-wal
db/sessions.db-shm

# Node
node_modules/
*.log
server.err

# Runtime log files
logs/

# User uploads
public/uploads/
uploads/

# Claude Code worktrees
.claude/worktrees/
